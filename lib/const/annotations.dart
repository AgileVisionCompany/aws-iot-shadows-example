
// @nullable annotation, used for marking fields/methods that may contain NULL values

// Build-in null-safety Dart feature is not supported right now by the stable Flutter SDK

// See the status of Null-safety here: https://flutter.dev/docs/null-safety
// It will be good to remove this annotation from the project as soon as Null-safety becomes stable.

class Nullable {
  const Nullable();
}
const Nullable nullable = Nullable();

